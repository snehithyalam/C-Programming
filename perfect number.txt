#include<stdio.h>
int main()
{
int n,i,sum=0,rem;
printf("enter the number:");
scanf("%d",&n);
for(i=1;i<n;i++)
{
rem=n%i;
if(rem==0)
{
sum=sum+i;	
}
}
if(sum==n)
printf("%d is perfect number",n);
else
printf(" %d is not perfect number",n);
}
